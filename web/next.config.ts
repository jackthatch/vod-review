const nextConfig = {
  // Monorepo: Vercel deploys the `web/` directory as the project root.
  // No special config needed for Supabase (env vars come from Vercel dashboard).
};

export default nextConfig;
